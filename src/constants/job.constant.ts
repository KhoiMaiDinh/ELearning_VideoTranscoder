export enum QueueName {
  TRANSCODER = 'tempvideobucketevent',
  VALIDATE = 'validate',
}

export enum QueuePrefix {
  VIDEO = 'video',
  IMAGE = 'image',
}

export enum JobName {
  TRANSCODE = 'transcode',
  VALIDATE = 'validate',
}
